export type TCanvas = CanvasRenderingContext2D | null
export type TLeftTopPoint = {
    x: number,
    y: number
} | 0;

export type TAllPOintPOsition = {
    tx: number,
    ty: number,
    bx: number,
    by: number
}